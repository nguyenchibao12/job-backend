PORT=5000
MONGO_URI=mongodb+srv://baoncse172645_db_user:tmhkhbJclm16YAlr@cluster0.abpj9lu.mongodb.net/myappdb?retryWrites=true&w=majority&appName=Cluster0
# --- Cấu hình JSON Web Token (JWT) ---
JWT_SECRET=a8f3b2e9d7c1a0b9e8f2d5c3b1a4e7d9f0c8b6a2d1e5f7c3a0b8e9d2c5b1a44
# <-- THAY ĐỔI DÒNG NÀY (RẤT QUAN TRỌNG!)
# Ý nghĩa: Đây là "chìa khóa bí mật" để tạo và xác thực token đăng nhập. Phải giữ bí mật tuyệt đối.
# Cách làm: Tạo một chuỗi ký tự ngẫu nhiên thật dài (ít nhất 32 ký tự, ví dụ dùng trang web tạo chuỗi ngẫu nhiên) và dán vào đây.
# Ví dụ: JWT_SECRET=a8f3b2e9d7c1a0b9e8f2d5c3b1a4e7d9f0c8b6a2d1e5f7c3a0b8e9d2c5b1a4

# --- Cấu hình Gửi Email (cho chức năng Quên mật khẩu) ---
EMAIL_SERVICE=gmail
# Giữ nguyên 'gmail' nếu bro dùng Gmail để gửi.
# Nếu dùng dịch vụ khác (SendGrid, Mailgun,...), thay 'gmail' bằng tên dịch vụ tương ứng (có thể cần cấu hình khác trong code nodemailer).

EMAIL_USERNAME=bao2k34@gmail.com
# <-- THAY ĐỔI DÒNG NÀY
# Ý nghĩa: Địa chỉ email Gmail đầy đủ mà backend sẽ dùng để gửi email đi.
# Cách làm: Thay bằng địa chỉ Gmail thật của bro (hoặc email tạo riêng cho app).

EMAIL_PASSWORD=tbqmyddoscwclmad
# <-- THAY ĐỔI DÒNG NÀY (RẤT QUAN TRỌNG!)
# Ý nghĩa: Mật khẩu Ứng dụng (App Password) gồm 16 ký tự của tài khoản Gmail trên. KHÔNG PHẢI mật khẩu Gmail thông thường.
# Cách làm:
#   1. Bật Xác minh 2 bước cho Gmail.
#   2. Tạo Mật khẩu ứng dụng (App Password) trong cài đặt bảo mật tài khoản Google. (Search Google: "tạo mật khẩu ứng dụng google")
#   3. Copy mật khẩu 16 chữ cái (viết liền, không dấu cách) và dán vào đây.

EMAIL_FROM="StudentWork Support <bao2k34@gmail.com>"
# <-- THAY ĐỔI EMAIL TRONG NGOẶC NHỌN
# Ý nghĩa: Tên và địa chỉ email hiển thị ở dòng "Người gửi" (From).
# Cách làm: Thay `email_gmail_cua_bro@gmail.com` bằng email bro đã điền ở `EMAIL_USERNAME`.
# --- CẤU HÌNH CLOUDINARY (Lưu trữ ảnh biên lai) ---
CLOUDINARY_CLOUD_NAME=dfbwmigj7
CLOUDINARY_API_KEY=335717914938965
CLOUDINARY_API_SECRET=FhMoGHlyxhjMp1kQD1VAyUwbIp4